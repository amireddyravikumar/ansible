- name: demo install multiple packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - git
    - mysql