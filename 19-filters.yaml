- name: demo in filters
  hosts: local
  connection: local
  vars:
    topics: Linux, Shell, Ansible, Terraform
  tasks:
  - name: convert string to list 
    debug:
      msg: "{{ topics | split(',') }}"
  - name: set default value
    debug:
      msg: "Hi, {{ GREETING | default('ravi') }}"