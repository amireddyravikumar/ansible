- name: demo on preference variables
  hosts: frontend
  vars:
    full_name: "Ravi from play level"
  vars_files:
  - course.yaml
  vars_prompt:
  - name: full_name
    prompt: "Please enter your full_name"
    private: false
        
  tasks:
  - name: print vars preference
    vars:
      #full_name: "Ravi from task level"
    debug:
      msg: "full_name = {{ full_name }}"

  # CMD args
  # Task Level
  # var 
  # prompt
  # play
  # inventory