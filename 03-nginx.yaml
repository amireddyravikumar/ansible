- name: install nginx
  hosts: frontend
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: install nginx
    ansible.builtin.package:
      name: service
      state: started 
      enabled: yes
