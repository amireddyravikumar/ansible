- name: demoon debug
  hosts: frontend # Target server
  tasks:  # tasks = modules=collections 
  - name: hello world
    debug:
      msg: "Hello Ansible"
  - name: hello world1
    debug:
      msg: "Hello Ansible1"